## FE

- no error message on failed login
- registration has see transparent panels, looks weird. Should be solid.
- Inconsistency in font usage + very small font in title bar (can we set some standards on size an type)
- progress (level/stats) is also see through, should be solid
- daily bonus and add quest buttons should have more simple styling (no weird fade)
- quest list and header not lined out straight (due to sidebar)
- move sidebar to the actual side, make it so that in extended status it does not push quest list aside. Quest list is the main item, it should be set in stone. 
- open quests should be at top of the list, when completing a quest, move it to the top of 'completed'
- make icosn for sidebar clearer - use sprite creation skill if necessary